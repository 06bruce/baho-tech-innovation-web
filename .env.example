# Copy to .env.local (development) or configure in your host's dashboard (production).
# No secrets belong in this file — every VITE_* value is public once built.

VITE_APP_ENV=development
VITE_API_BASE_URL=/api
VITE_FRONTEND_URL=http://localhost:5173
