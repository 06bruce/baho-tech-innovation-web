# Frontend Development Environment
# This file is in .gitignore - local changes won't be committed

# API Configuration
# In development, Vite proxy to backend is used (/api -> http://127.0.0.1:3001)
# You can override by setting this variable, but not needed for dev
# VITE_API_BASE_URL=http://127.0.0.1:3001/api
